<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Certificate of Internship</title>
  <style>
    @import url("https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&display=swap");
    @import url("https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap");

    html, body {
      width: 100%;
      height: 100%;
      margin: 0;
      padding: 0;
      background: #ffffff;
    }

    .certificate-container {
      width: 100%;
      height: 100vh;
      box-sizing: border-box;
      background: #ffffff;
      position: relative;
      text-align: center;
      box-shadow: 0 25px 60px rgba(0, 0, 0, 0.5);
      border: 6px solid #c8a864;
      overflow: hidden;
    }

    /* Corner floral ornaments */
    .corner-flower {
      position: absolute;
      width: 80px;
      height: 80px;
      border-radius: 50%;
      border: 4px solid #d4af37;
      box-shadow: inset 0 0 10px #f4e5b8;
      background: radial-gradient(circle, #f4e5b8 0%, #d4af37 100%);
    }
    .corner-top-left { top: -40px; left: -40px; }
    .corner-top-right { top: -40px; right: -40px; }
    .corner-bottom-left { bottom: -40px; left: -40px; }
    .corner-bottom-right { bottom: -40px; right: -40px; }

    .watermark {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 120px;
      color: rgba(200, 168, 100, 0.05);
      font-weight: 700;
      pointer-events: none;
      font-family: "Playfair Display", serif;
      letter-spacing: 5px;
      z-index: 0;
    }

    .content-wrapper {
      position: relative;
      z-index: 1;
      padding: 20px 30px;
    }

    .certificate-title {
      font-family: "Playfair Display", serif;
      font-size: 64px;
      font-weight: 700;
      color: #1a3a5c;
      margin: 0 0 5px 0;
      letter-spacing: 4px;
      text-transform: uppercase;
    }

    .subtitle {
      font-family: "Playfair Display", serif;
      font-size: 28px;
      font-weight: 600;
      color: #c8a864;
      margin: 0 0 30px 0;
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    .presented-to {
      font-family: "Playfair Display", serif;
      font-size: 18px;
      color: #c8a864;
      margin: 0 0 15px 0;
      font-weight: 600;
    }

    .recipient-name {
      font-family: "Playfair Display", serif;
      font-size: 42px;
      font-weight: 700;
      color: #1a3a5c;
      margin: 0 0 25px 0;
      letter-spacing: 2px;
      text-transform: uppercase;
      border-bottom: 3px solid #c8a864;
      display: inline-block;
      padding-bottom: 8px;
      min-width: 350px;
    }

    .description {
      font-family: "Playfair Display", serif;
      font-size: 15px;
      line-height: 1.6;
      color: #1a3a5c;
      margin: 0 auto 40px;
      max-width: 750px;
      font-weight: 500;
    }

    .signature-section {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 50px;
      position: relative;
    }

    .signature-block {
      text-align: center;
      flex: 1;
    }

    .signature-line {
      font-family: "Great Vibes", cursive;
      font-size: 36px;
      color: #1a3a5c;
      margin-bottom: 8px;
    }

    .signature-name {
      font-family: "Playfair Display", serif;
      font-size: 16px;
      font-weight: 700;
      color: #c8a864;
      margin: 0;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .signature-title {
      font-family: "Playfair Display", serif;
      font-size: 14px;
      color: #1a3a5c;
      margin: 5px 0 0 0;
      font-weight: 400;
    }

    /* Static flower center for PDF */
    .flower-divider {
      width: 200px;
      height: 200px;
      margin: 0 20px;
      position: relative;
      flex-shrink: 0;
    }
    .flower-divider span {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 20px;
      height: 50px;
      background: radial-gradient(circle at 50% 30%, #f4e5b8 0%, #d4af37 100%);
      border-radius: 50% 50% 0 0;
      transform-origin: bottom center;
    }
    .flower-divider span:nth-child(1) { transform: translate(-50%, -50%) rotate(0deg); }
    .flower-divider span:nth-child(2) { transform: translate(-50%, -50%) rotate(45deg); }
    .flower-divider span:nth-child(3) { transform: translate(-50%, -50%) rotate(90deg); }
    .flower-divider span:nth-child(4) { transform: translate(-50%, -50%) rotate(135deg); }
    .flower-divider span:nth-child(5) { transform: translate(-50%, -50%) rotate(180deg); }
    .flower-divider span:nth-child(6) { transform: translate(-50%, -50%) rotate(225deg); }
    .flower-divider span:nth-child(7) { transform: translate(-50%, -50%) rotate(270deg); }
    .flower-divider span:nth-child(8) { transform: translate(-50%, -50%) rotate(315deg); }
  </style>
</head>
<body>
  <div class="certificate-container">
    <!-- Curved corners -->
    <div class="corner-flower corner-top-left"></div>
    <div class="corner-flower corner-top-right"></div>
    <div class="corner-flower corner-bottom-left"></div>
    <div class="corner-flower corner-bottom-right"></div>

    <div class="watermark">XYNAPSE</div>

    <div class="content-wrapper">
      <h1 class="certificate-title">CERTIFICATE</h1>
      <h2 class="subtitle">OF ACHIEVEMENT</h2>
      
      <p class="presented-to">This certificate is proudly presented to</p>
      <h3 class="recipient-name"><%= name %></h3>
      
      <p class="description">
        For successfully completing the internship in <strong><%= department %></strong> at <strong>XYNAPSE SYSTEMS</strong> 
        from <%= certificateData.startDate %> to <%= certificateData.endDate %>. This achievement recognizes exceptional 
        dedication, outstanding performance, and valuable contributions to the organization.
      </p>

      <div class="signature-section">
        <div class="signature-block">
          <div class="signature-line">Adora Montminy</div>
          <p class="signature-name"><%= certificateData.signatoryName || 'ADORA MONTMINY' %></p>
          <p class="signature-title"><%= certificateData.signatoryDesignation || 'Director' %></p>
        </div>

        <!-- Flower in the center -->
        <div class="flower-divider">
          <span></span><span></span><span></span><span></span>
          <span></span><span></span><span></span><span></span>
        </div>

        <div class="signature-block">
          <div class="signature-line">Claudia Alves</div>
          <p class="signature-name">CLAUDIA ALVES</p>
          <p class="signature-title">Manager</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
